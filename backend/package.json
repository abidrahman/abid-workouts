{
  "name": "abid-workouts-backend",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions backend for Abid Workouts with COROS OAuth integration",
  "main": "functions/index.js",
  "type": "module",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test 'tests/**/*.test.js'"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
